{
  "name": "routergen",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "check": "biome check --apply .",
    "dev": "concurrently \"sleep 3 && nodemon build/main.js\" \"pnpm run build:watch\" --names node,tsc --prefix-colors magenta,cyan",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@biomejs/biome": "^1.5.3",
    "nodemon": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.20",
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
